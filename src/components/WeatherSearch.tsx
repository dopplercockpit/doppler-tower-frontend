// src/components/WeatherSearch.tsx
interface Props {
  onSearch: (location: string) => void;
}
export default function WeatherSearch({ onSearch }: Props) {
  return null; // Replace with real UI later
}